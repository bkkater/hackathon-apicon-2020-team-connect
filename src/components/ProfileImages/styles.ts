import { StyleSheet, Image } from 'react-native';

const styles = StyleSheet.create({
    imageGroup: {
        flexDirection: 'row',
        marginTop: 3
    },
    image: {
        marginRight: 2
    }
})

export default styles;